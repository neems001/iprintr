# Server-only credentials. Use development resources for local testing.
# Preferred Blob authentication (refresh local OIDC tokens when expired):
BLOB_STORE_ID=
VERCEL_OIDC_TOKEN=
# Alternative legacy Blob authentication:
BLOB_READ_WRITE_TOKEN=
HF_TOKEN=
GEMINI_API_KEY=
IPRINTR_DATABASE_URL=

# OAuth is optional. Guests can still generate and keep history without it.
# Create a Clerk development instance and enable Google, GitHub, or both.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
